import {
  loginUser,
  updateUser,
  registerUser,
} from "@controller/auth.controller";

import {
  uploadFile,
  deleteFile,
  getFiles,
} from "@controller/storage.controller";

import {
  getAllTags,
  createNewTags,
  updateTag,
} from "@controller/tags.controller";

export {
  loginUser,
  updateUser,
  registerUser,
  uploadFile,
  deleteFile,
  getFiles,
  getAllTags,
  createNewTags,
  updateTag,
};
