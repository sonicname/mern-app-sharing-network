import {
  loginUser,
  updateUser,
  registerUser,
} from "@controller/auth.controller";

import {
  uploadFile,
  deleteFile,
  getFiles,
} from "@controller/storage.controller";

export {
  loginUser,
  updateUser,
  registerUser,
  uploadFile,
  deleteFile,
  getFiles,
};
